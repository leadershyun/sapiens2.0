# Sapiens2.0 dependencies
# Python 3.8+ recommended
# Install: pip install -e .  (recommended — installs the 'sapiens' CLI command)
# Or:      pip install -r requirements.txt

requests>=2.28.0

# Optional: Discord bot integration (required only for 'sapiens wakeup --discord')
# Install with: pip install "discord.py>=2.0.0"
# discord.py>=2.0.0
